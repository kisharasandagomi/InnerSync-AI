# ML Architecture

## Status

Not yet populated. Will document the `ml_pipeline/` module structure,
artifact versioning scheme, and the artifact contract consumed by
`backend/app/ml` (model `.pkl`, fitted preprocessors, `feature_schema.json`,
`shap_config.json`).
