# System Architecture

## Status

Not yet populated. Will document the Research World / Production World
separation (see `CLAUDE.md`), component boundaries, and data flow between
`ml_pipeline/`, `backend/app/`, and `frontend/`.
