"""FastAPI entrypoint for the InnerSync AI backend (Production World).

Deferred: implementation begins at PROJECT_ROADMAP.md Phase 6. This file
only loads artifacts produced by ml_pipeline/ — it must never train a model.
"""
