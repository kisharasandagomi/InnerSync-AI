# Changelog

All notable changes to this project are documented here.

## [Unreleased]

### Added
- Initial repository scaffold: full directory structure for `ml_pipeline/`,
  `backend/`, `frontend/`, `docs/`, and `deployment/`.
- `.gitignore`, `environment.yml` (exported from conda env `mainks`).
- Git repository initialized.
- `ml_pipeline/notebooks/01_EDA.ipynb` scaffolded for initial exploration of
  the Kaggle "Student Stress Factors" dataset.
